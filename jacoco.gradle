apply plugin: "jacoco"
jacoco {
    toolVersion = "0.8.3"
}

def debugTree = fileTree(dir: "${buildDir}/classes/kotlin/main")

jacocoTestReport {
    reports {
        html.enabled = true
    }

    afterEvaluate {
        getClassDirectories().from(debugTree)
    }
}

jacocoTestCoverageVerification {
    violationRules {
        rule {
            limit {
                element = 'BUNDLE'
                counter = "LINE"
                minimum = 1
            }
        }

        afterEvaluate {
            getClassDirectories().from(debugTree)
        }
    }
}

test { finalizedBy jacocoTestReport }
check.dependsOn(jacocoTestCoverageVerification)